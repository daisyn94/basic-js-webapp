version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          command: "npm install && npm test"

workflows:
  test-workflow:
    jobs:
      - test
